PORT=3000
MONGODB_URI=mongodb://localhost:27017/site-constructor

# Row output strategy: console | kafka (Labor Work 4 — switch without code change)
ROW_OUTPUT_STRATEGY=console

# Kafka (only when ROW_OUTPUT_STRATEGY=kafka); uses persistence/kafka KafkaClientFactory
# KAFKA_BROKERS=localhost:9092
# KAFKA_CLIENT_ID=site-constructor
# KAFKA_TOPIC=csv-rows
